# Essentials Configuration
# This is a basic config that allows ops to use all commands

# Allow ops to bypass all permission checks
allow-direct-login: true

# Enable all commands for ops
command-costs:
  sethome: 0
  home: 0
  warp: 0
  setwarp: 0
  tpa: 0
  claim: 0
  rtp: 0
